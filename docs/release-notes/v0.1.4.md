# Claude Manager v0.1.4 发布说明

**发布日期**: 2026-02-27
**版本**: v0.1.4
**类型**: 功能改进版本

## 新增功能

### 1. 改进的启动脚本

**功能描述**:
start.sh 现在可以同时启动前端和后端服务，提供完整的开发体验。

**主要改进**:
- ✅ 同时启动前端（5173）和后端（8000）服务
- ✅ 后端在后台运行，前端在前台运行
- ✅ 主要入口显示为前端 http://localhost:5173
- ✅ 本地开发使用 ENV=development（启用 reload）
- ✅ 自动安装前端依赖（npm install）
- ✅ 按 Ctrl+C 时自动停止所有服务
- ✅ 后端日志输出到 docs/logs/backend.log

**启动信息**:
```
============================================
✅ Claude Manager is running!
============================================

🌐 Frontend: http://localhost:5173
🔧 Backend API: http://localhost:8000
📚 API Docs: http://localhost:8000/docs

Press Ctrl+C to stop all servers
============================================
```

## 平台支持

本版本提供两个平台的发布包：

### macOS (ARM64)
- 文件名: `claude-manager-v0.1.4-macos-arm64.tar.gz`
- 大小: 17M
- SHA256: `18bdb86e6d8c0a64c845107a918c69851ef025da50c9f6e298c5e4c693c87a61`
- 适用于: Apple Silicon Mac (M1/M2/M3)

### Linux (x86_64)
- 文件名: `claude-manager-v0.1.4-linux-x86_64.tar.gz`
- 大小: 17M
- SHA256: `c15dba97d6edb1e022c7fb5e8168e289a1e0e9af612b8a43e177a883bfa0991b`
- 适用于: Linux x86_64 系统

## 安装说明

### 首次安装

1. 下载对应平台的发布包

2. 解压：
   ```bash
   tar -xzf claude-manager-v0.1.4-{platform}-{arch}.tar.gz
   cd claude-manager-v0.1.4-{platform}-{arch}
   ```

3. 运行启动脚本：
   ```bash
   ./start.sh
   ```

4. 首次运行会自动：
   - 创建 Python 虚拟环境
   - 安装 Python 依赖
   - 安装前端依赖（npm install）
   - 从 .env.example 创建 .env 文件

5. 编辑配置（如需要）：
   ```bash
   nano backend/.env
   ```

6. 访问应用：
   - 前端界面: http://localhost:5173
   - API 文档: http://localhost:8000/docs

### 从 v0.1.3 升级

如果你已经安装了 v0.1.3，建议重新安装：

1. 备份数据库（如果有重要数据）：
   ```bash
   cp backend/claude_manager.db ~/claude_manager_backup.db
   ```

2. 删除旧版本，按照上述"首次安装"步骤安装新版本

3. 恢复数据库：
   ```bash
   cp ~/claude_manager_backup.db backend/claude_manager.db
   ```

## 系统要求

### macOS
- macOS 12.0 或更高版本
- Apple Silicon (M1/M2/M3) 或 Intel 处理器
- Python 3.8+
- Node.js 16+

### Linux
- 主流 Linux 发行版（Ubuntu 20.04+, Debian 11+, CentOS 8+）
- x86_64 架构
- Python 3.8+
- Node.js 16+

## 验证安装

启动成功后，验证以下内容：

1. 前端界面可访问: http://localhost:5173
2. API 文档可访问: http://localhost:8000/docs
3. 健康检查通过: http://localhost:8000/health

## 已知问题

- 首次启动时安装依赖可能需要几分钟
- 需要确保 5173 和 8000 端口未被占用

## 变更日志

### 改进
- 改进 start.sh 启动脚本，同时启动前端和后端
- 优化启动信息显示，突出前端入口
- 添加自动依赖安装功能
- 添加后端日志输出到文件

### 修复
- 修复 v0.1.3 中的发布包运行问题
- 修复环境变量配置问题

## 下一版本计划

- 前端功能完善
- WebSocket 实时监控优化
- Workflow 执行引擎增强

## 技术支持

如遇到问题，请提供以下信息：

1. 操作系统版本
2. Python 版本：`python3 --version`
3. Node.js 版本：`node --version`
4. 错误日志：`backend/docs/logs/backend.log`
5. 启动输出：完整的 `./start.sh` 输出

## 文件校验

### macOS ARM64
```
SHA256: 18bdb86e6d8c0a64c845107a918c69851ef025da50c9f6e298c5e4c693c87a61
```

### Linux x86_64
```
SHA256: c15dba97d6edb1e022c7fb5e8168e289a1e0e9af612b8a43e177a883bfa0991b
```

验证文件完整性：
```bash
shasum -a 256 claude-manager-v0.1.4-{platform}-{arch}.tar.gz
```
